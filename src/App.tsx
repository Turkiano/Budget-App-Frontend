import React, { useState } from 'react';

const TodoApp = () => {
  cosnt[(todos, setTods)] = useState([
    { id: 1, text: 'learn React', isEditing: false },
    { id: 2, text: 'Build a Todo App', isEditing: false },
  ]);

  const [editText, setEditText] = useState('');
};
